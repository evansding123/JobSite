import React, { useState } from 'react';
import styled from 'styled-components';
import axios from 'axios';

const Notes = () => {
  //assume email is passed to notes
  const [text, handleChange] = usestate('');
  

};

export default Notes;